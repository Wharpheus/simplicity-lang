{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "java (build)",
            "paths": [
                "${workspace}"
            ],
            "isFullBuild": true,
            "group": "build",
            "problemMatcher": [],
            "label": "build",
            "detail": "$(tools) Build all the Java projects in workspace."
        }
    ]
}